import { Pipe, PipeTransform } from '@angular/core';

@Pipe({
  name: 'filter'
})
export class FilterPipe implements PipeTransform {
  transform(items: any[], state: string): any[] {
    if (!items || !state) {
      return items;
    }
    return items.filter(item => item.estado === state);
  }
}
